language: java

sudo: false

script:
    - gradle build 
    - touch deployTest.txt

deploy:
  provider: releases
  api-key: "GITHUB OAUTH TOKEN"
  file: 
    - deployTest.txt
  skip_cleanup: true
  on:
    tags: true 
