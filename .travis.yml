language: java

sudo: false

script:
    - gradle build 
    - touch deployTest.txt

deploy:
    provider: pages
    file:  
      - deployTest.txt
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    on:
      tags: true
